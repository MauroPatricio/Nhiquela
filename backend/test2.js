import mongoose from 'mongoose'; import dotenv from 'dotenv'; dotenv.config(); import RequestService from './models/RequestServiceModel.js'; mongoose.connect(process.env.MONGODB_URI).then(async () => { const reqs = await RequestService.find({ status: { $in: ['Pendente', 'Aceite pelo entregador', 'A Caminho', 'Em andamento'] } }).lean(); console.log(JSON.stringify(reqs, null, 2)); process.exit(0); });
